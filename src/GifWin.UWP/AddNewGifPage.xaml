<Page
    x:Class="GifWin.UWP.AddNewGifPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GifWin.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="10 0 0 0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" FontSize="36" Text="Add New GIF" Margin="0 0 0 10"/>

        <TextBox Grid.Row="1" Text="{Binding Url, Mode=TwoWay}" PlaceholderText="Image URL" />
        <TextBox Grid.Row="2" Text="{Binding TagString, Mode=TwoWay}" PlaceholderText="Comma-separated tags" Margin="0 10 0 0"/>
        
        <Image Grid.Row="3" Margin="0 10 0 0">
            <Image.Source>
                <BitmapImage UriSource="{Binding Url}" />
            </Image.Source>
        </Image>

        <Button Grid.Row="4" Command="{Binding AddGif}" CommandParameter="{Binding}" Content="Add GIF" Margin="0 10 0 0" />
    </Grid>
</Page>
